{
    "name": "CargoDesk",
    "scripts": {
        "dev:frontend": "npm run dev --prefix frontend",
        "dev:back": "npm run dev --prefix back",
        "dev": "concurrently \"npm:dev:back\" \"npm:dev:frontend\""
    },
    "devDependencies": {
        "concurrently": "^9.0.0"
    }
}